version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: agent-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: agent_db
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 10s
      timeout: 5s
      retries: 5

  # Uncomment to run API with Docker
  # api:
  #   build:
  #     context: ..
  #     dockerfile: infra/Dockerfile.api
  #   container_name: agent-api
  #   restart: unless-stopped
  #   ports:
  #     - '3001:3001'
  #   environment:
  #     DATABASE_URL: postgresql://postgres:postgres@db:5432/agent_db
  #     JWT_SECRET: your-super-secret-jwt-key
  #     CORS_ORIGIN: http://localhost:3000
  #   depends_on:
  #     db:
  #       condition: service_healthy

  # Uncomment to run BFF with Docker
  # bff:
  #   build:
  #     context: ..
  #     dockerfile: infra/Dockerfile.bff
  #   container_name: agent-bff
  #   restart: unless-stopped
  #   ports:
  #     - '3002:3002'
  #   environment:
  #     UPSTREAM_API_URL: http://api:3001
  #     JWT_SECRET: your-super-secret-jwt-key
  #     CORS_ORIGIN: http://localhost:3000
  #   depends_on:
  #     - api

volumes:
  postgres_data:
